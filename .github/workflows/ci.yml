name: CI

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: 20
          cache: npm
      - run: npm ci
      - run: npm test
      - name: "Dependency audit (report only: upstream SDK currently has advisories)"
        continue-on-error: true
        run: npm audit --omit=dev --audit-level=high
